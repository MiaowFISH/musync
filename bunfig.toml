[install]
# Frozen lockfile for reproducible builds
frozen = false

[install.cache]
# Cache packages in .bun directory
dir = ".bun/cache"

[test]
# Test configuration
coverage = true
coverageDir = "coverage"

[run]
# Runtime configuration
silent = false
